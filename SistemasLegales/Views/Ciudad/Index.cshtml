@model IEnumerable<Ciudad>
@{
    ViewData["Titulo"] = "CIUDADES";
    ViewData["Subtitulo"] = "LISTADO";

}
@section Migas{
    <li><a asp-action="Index">Ciudades</a></li>
    <li class="active">Listado</li>
}
@section CSS{
    @{await Html.RenderPartialAsync("_DatatablesCSSPartial");}
}

@{
    if (User.Identity.Name == Perfiles.Gestor)
    {
        <div class="row">
            <div class="col-lg-2 col-sm-4 col-xs-12">
                <a asp-action="Gestionar" class="btn btn-primary waves-effect">NUEVA</a>
            </div>
        </div>
    }
}

<table id="datatable-responsive" class="table table-striped table-bordered dt-responsive nowrap dataTable" cellspacing="0">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().Nombre)
            </th>
            @{
                if (User.Identity.Name == Perfiles.Gestor)
                {
                    <th>
                        Opciones
                    </th>
                }
            }
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    @{
                        if (User.Identity.Name == Perfiles.Gestor)
                        {
                            <td>
                                <a asp-action="Gestionar" asp-route-id="@item.IdCiudad">Editar</a> |
                                <a href="javascript:void(0);" class="btnEliminar" id="@item.IdCiudad" data-descripcion="&#191;Desea eliminar la Ciudad... ?">Eliminar</a>
                            </td>
                        }
                    }
                </tr>
            }
        }
    </tbody>
</table>
@{
    if (User.Identity.Name == Perfiles.Gestor)
    {
        await Html.RenderPartialAsync("_Eliminar");
    }
}

@section Scripts{
    @{await Html.RenderPartialAsync("_DatatablesJSPartial");}
    @{await Html.RenderPartialAsync("_BootboxPartial");}
    <script src="~/js/jqueryDatatables/jqueryDatatables.min.js"></script>
}